<Window x:Class="TranslateClient2.Views.Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TranslateClient2.Views.Main"
        xmlns:tb="http://www.hardcodet.net/taskbar" 
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" Loaded="Window_Loaded">
    <DockPanel>

        <tb:TaskbarIcon
            ToolTipText="i am in tray!"
            IconSource="/TrayIcon.ico"
        ></tb:TaskbarIcon>

        <Grid Background="Black">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <TextBox x:Name="InputControl" Grid.Row="0" TextChanged="InputControl_TextChanged" Margin="10 10 10 5"></TextBox>
            <TextBox x:Name="TranslatedControl" Grid.Row="1" IsReadOnly="True" Margin="10 5 10 10"></TextBox>
        </Grid>
    </DockPanel>

</Window>

